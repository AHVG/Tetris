#ifndef _H_CONSTANTS
#define _H_CONSTANTS

const float TETROMINO_SIZE = 25.0;

const float WIDTH = 10;
const float HEIGHT = 20;

const float WINDOW_WIDTH = TETROMINO_SIZE * WIDTH + 400;
const float WINDOW_HEIGHT = TETROMINO_SIZE * HEIGHT + 400;

#endif